{
  "name": "@xianyu/database",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:migrate": "node scripts/migrate.ts",
    "db:seed": "NODE_ENV=test node scripts/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "embedded-postgres": "16.4.0-beta.14",
    "prisma": "^7.9.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "pg": "^8.22.0",
    "@xianyu/pricing-engine": "workspace:*"
  }
}
